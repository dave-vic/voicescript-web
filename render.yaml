services:
  - type: web
    name: voicescript-web
    runtime: python
    buildCommand: "bash build.sh"
    startCommand: "python main.py"
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
